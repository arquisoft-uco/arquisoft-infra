# ==============================================================================
# Arquisoft - Docker Compose Observability Services
# ==============================================================================
# Stack de observabilidad: Prometheus, Loki, Grafana
# Métricas, logs centralizados y dashboards
#
# Uso: docker-compose -f docker-compose.yaml -f docker-compose.observability.yaml up -d
# ==============================================================================

services:
  # ============================================================================
  # Prometheus - Recolección de métricas
  # ============================================================================
  prometheus:
    image: prom/prometheus:v2.53.3
    container_name: arquisoft-prometheus
    restart: unless-stopped
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--web.enable-lifecycle'
    volumes:
      - prometheus-data:/prometheus
      - ./configs/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./configs/prometheus/alerts.yml:/etc/prometheus/alerts.yml:ro
    networks:
      - arquisoft-network
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 200M
          cpus: '0.25'
        reservations:
          memory: 100M
          cpus: '0.1'

  # ============================================================================
  # Loki - Agregación de logs
  # ============================================================================
  loki:
    image: grafana/loki:2.9.10
    container_name: arquisoft-loki
    restart: unless-stopped
    command: -config.file=/etc/loki/loki-config.yaml
    volumes:
      - loki-data:/loki
      - ./configs/loki/loki-config.yaml:/etc/loki/loki-config.yaml:ro
    networks:
      - arquisoft-network
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:3100/ready || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 200M
          cpus: '0.25'
        reservations:
          memory: 100M
          cpus: '0.1'

  # ============================================================================
  # Promtail - Recolector de logs (envía a Loki)
  # ============================================================================
  promtail:
    image: grafana/promtail:2.9.10
    container_name: arquisoft-promtail
    restart: unless-stopped
    command: -config.file=/etc/promtail/promtail-config.yaml
    volumes:
      - ./configs/promtail/promtail-config.yaml:/etc/promtail/promtail-config.yaml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - traefik-logs:/var/log/traefik:ro
    networks:
      - arquisoft-network
    depends_on:
      loki:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "test -f /tmp/positions.yaml || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 100M
          cpus: '0.1'

  # ============================================================================
  # Grafana - Dashboards y visualización
  # ============================================================================
  grafana:
    image: grafana/grafana:10.4.12
    container_name: arquisoft-grafana
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:?GRAFANA_ADMIN_USER requerido - ejecutar scripts/setup-env.sh}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:?GRAFANA_ADMIN_PASSWORD requerido - ejecutar scripts/setup-env.sh}
      GF_USERS_ALLOW_SIGN_UP: false
      GF_SERVER_ROOT_URL: ${GRAFANA_ROOT_URL:-http://localhost:3000}
      GF_INSTALL_PLUGINS: grafana-clock-panel,grafana-piechart-panel
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER requerido}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD requerido}
      POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB requerido}
    volumes:
      - grafana-data:/var/lib/grafana
      - ./configs/grafana/provisioning:/etc/grafana/provisioning:ro
    networks:
      - arquisoft-network
    depends_on:
      prometheus:
        condition: service_healthy
      loki:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:3000/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 150M
          cpus: '0.25'
        reservations:
          memory: 100M
          cpus: '0.1'
