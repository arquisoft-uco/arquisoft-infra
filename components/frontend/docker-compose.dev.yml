# =============================================================================
# Componente: frontend — overlay DESARROLLO
# =============================================================================
services:
  frontend:
    restart: unless-stopped
    ports:
      - "127.0.0.1:8088:${FRONTEND_PORT:-80}"
    labels:
      - "traefik.enable=false"
