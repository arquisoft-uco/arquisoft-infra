# Componente proxy (Traefik) — variables relevantes
# La fuente real es el .env de la raíz del repo.
DOMAIN=arquisoft.localhost
ACME_EMAIL=admin@tu-dominio.com   # solo prod (Let's Encrypt)
TIMEZONE=America/Bogota
