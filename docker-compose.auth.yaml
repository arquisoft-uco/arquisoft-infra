# ==============================================================================
# Arquisoft - Docker Compose Auth Services
# ==============================================================================
# Servicios de autenticación: Keycloak
# Integración con Azure AD de la UCO via SAML/OIDC
#
# Uso: docker-compose -f docker-compose.yaml -f docker-compose.auth.yaml up -d
# ==============================================================================

services:
  # ============================================================================
  # Keycloak - Identity Provider (SSO, OAuth 2.0, OIDC)
  # ============================================================================
  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    container_name: arquisoft-keycloak
    restart: unless-stopped
    command: start-dev --import-realm
    environment:
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN:?KEYCLOAK_ADMIN requerido - ejecutar scripts/setup-env.sh}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:?KEYCLOAK_ADMIN_PASSWORD requerido - ejecutar scripts/setup-env.sh}
      
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:?POSTGRES_DB requerido}
      KC_DB_USERNAME: ${POSTGRES_USER:?POSTGRES_USER requerido}
      KC_DB_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD requerido}
      KC_DB_SCHEMA: keycloak
      
      KC_FEATURES: token-exchange,admin-fine-grained-authz
      KC_HEALTH_ENABLED: true
      KC_METRICS_ENABLED: true
      
      KC_HOSTNAME_STRICT: false
      KC_HOSTNAME_STRICT_HTTPS: false
      KC_HTTP_ENABLED: true
    volumes:
      - ./configs/keycloak/realm-arquisoft.json:/opt/keycloak/data/import/realm-arquisoft.json:ro
    networks:
      - arquisoft-network
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/9000;echo -e \"GET /health/ready HTTP/1.1\r\nhost: localhost\r\nConnection: close\r\n\r\n\" >&3;cat <&3 | grep -q '\"status\": \"UP\"'"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    deploy:
      resources:
        limits:
          memory: 768M
          cpus: '1'
        reservations:
          memory: 512M
          cpus: '0.5'
